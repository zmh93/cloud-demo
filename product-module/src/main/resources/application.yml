server:
  port: 7002
spring:
  application:
    name: product-module
  profiles:
    active: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka
    fetch-registry: true
    register-with-eureka: true